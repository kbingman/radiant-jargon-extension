%h1 Remove User

%p 
  = "Are you sure you want to <strong class='warning'>permanently</strong> remove the following user?"[:remove_user_warning]

%table.index#users{:cellpadding=>0, :cellspacing=>0, :border=>0}
  %tbody
    %tr.node.level-1
      %td.user
        %span.title= @user.name

- form_tag do
  %p.buttons
    %input.button{:type=>"submit", :value=>"Delete User"[:delete_user]}/
    = "or"[:or]
    = link_to 'Cancel'[:cancel], user_index_url